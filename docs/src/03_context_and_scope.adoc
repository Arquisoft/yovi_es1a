ifndef::imagesdir[:imagesdir: ../images]

[[section-context-and-scope]]
== Context and Scope

ifdef::arc42help[]
[role="arc42help"]
****
.Contents
Context and scope - as the name suggests - delimits your system (i.e. your scope) from all its communication partners
(neighboring systems and users, i.e. the context of your system). It thereby specifies the external interfaces.

If necessary, differentiate the business context (domain specific inputs and outputs) from the technical context (channels, protocols, hardware).
****
endif::arc42help[]

=== Business Context

ifdef::arc42help[]
[role="arc42help"]
****
.Contents
Specification of *all* communication partners (users, IT-systems, ...) with explanations of domain specific inputs and outputs or interfaces.
****
endif::arc42help[]

[plantuml]
----
@startuml
skinparam componentStyle uml2

actor "Player" as player
actor "Administrator" as admin
actor "Bot (AI)" as bot
node "Gatling" as gatling

component  "Yovi System" as yovi

player --> yovi : Registers, configures matches,\nplays and checks stats
admin --> yovi : Monitors system health & metrics
bot --> yovi : Consults match state and\nmakes automatic moves
gatling --> yovi : Simulates user traffic\n(Load Testing)

@enduml
----

**Diagram elements:**

* **Player:** The end-user who interacts with the web application to play games (against bots or other players in real-time) and manage their profile.
* **Administrator:** The technical user responsible for overseeing the system's stability and performance via the internal monitoring dashboards.
* **Bot (AI):** Conceptual actor representing the internal artificial intelligence engine that plays against the user.
* **Gatling:** An automated testing tool used to simulate concurrent user traffic to ensure the system's reliability and to find performance bottlenecks.
* **Yovi:** The complete system boundary. It acts as a unified solution that encapsulates the game engines, real-time communications, user management, data persistence, and the observability infrastructure.

=== Technical Context

ifdef::arc42help[]
[role="arc42help"]
****
.Contents
Technical interfaces (channels and transmission media) linking your system to its environment. In addition a mapping of domain specific input/output to the channels, i.e. an explanation which I/O uses which channel.
****
endif::arc42help[]

[plantuml]
----
@startuml
skinparam componentStyle uml2
skinparam componentBackgroundColor white
skinparam packageBackgroundColor #F2F2F2
skinparam defaultTextAlignment center

actor "Player" as player
actor "Administrator" as admin
node "Load Tester" as gatling

rectangle "Web Browser" as browser {
    component "React App (SPA)" as spa
}

node "Docker Host (IP: 20.199.88.71)" {
    component "Nginx\n(:80 Redirect, :443 SSL)" as nginx
    
    package "monitor-net (Docker Bridge)" {
        component "webapp\n(Port 80)" as webapp
        component "users\n(Node.js API - Port 3000)" as users
        component "gamey\n(Rust API - Port 4000)" as gamey
        component "multiplayer\n(Socket.io - Port 5000)" as multi

        component "Grafana\n(:9091)" as graf
        component "Prometheus\n(:9090)" as prom
    }
}

database "MongoDB" as mongo

player -> browser : Interacts
browser -right-> nginx : HTTPS [443] / WSS [443]
gatling -down-> nginx : Simulates Traffic\n[HTTPS]

nginx --> webapp : " / "
nginx --> users : " /api/ \n /api/bot/ \n /api/matches/ "
nginx --> gamey : " /api/gamey/ "
nginx --> multi : " /socket.io/ (Upgrade: websocket)"

spa .up.> nginx : HTTP / WS requests
users -right-> gamey : Internal HTTP requests\n(RUST_API_URL)

users --> mongo : Reads/Writes
gamey --> mongo : Reads/Writes
multi --> mongo : Reads/Writes

admin --> graf : HTTP [:9091]
admin --> prom : HTTP [:9090]
graf -> prom : Queries Data
prom -up-> users : Scrapes Metrics

@enduml
----

The Yovi system operates behind a reverse proxy that acts as the single point of entry, providing secure interfaces for external communication and managing a private Docker bridge network (`monitor-net`) for internal microservices. The communication channels are detailed below:

* **External Access Interfaces (Public):**
** **Nginx Reverse Proxy & TLS Termination:** Binds to ports `80` (redirects to HTTPS) and `443` (SSL). It uses configured certificates (`cert.pem`, `key.pem`) to encrypt all traffic from Players and Gatling load tests.
** **Frontend Delivery (`/`):** Nginx routes root traffic to the `webapp` container (port 80) to serve the React Single Page Application.
** **Node.js User & Bot API (`/api/`, `/api/bot/`, `/api/matches/`):** Handled by the `users` service on internal port `3000`. This includes authentication, user stats, and the artificial intelligence bot logic engine.
** **Rust Game API (`/api/gamey/`):** Routed to the `gamey` service on internal port `4000` for complex game mechanics calculation.
** **Real-Time Multiplayer (`/socket.io/`):** Nginx upgrades HTTP connections to WebSockets and routes them to the `multiplayer` service on internal port `5000`.

* **Internal Interfaces (Private `monitor-net` Network):**
** **Microservice Communication:** The `users` service communicates directly with the `gamey` service using the internal Docker DNS (`http://gamey:4000`) defined via the `RUST_API_URL` environment variable.
** **Monitoring & Observability:** Prometheus (exposed on port `9090`) scrapes metrics from the internal services. Grafana (exposed on port `9091`) queries Prometheus to build visual dashboards accessed by the Administrator.
** **Data Persistence:** The core backend services (`users`, `gamey`, `multiplayer`) connect to an internal MongoDB instance to persist user profiles, match history, and active session data.