ifndef::imagesdir[:imagesdir: ../images]

[[section-building-block-view]]

== Building Block View

ifdef::arc42help[]
[role="arc42help"]
****
.Content
The building block view shows the static decomposition of the system into building blocks...
****
endif::arc42help[]

=== Whitebox Overall System (Level 1)

The following diagram shows the main high-level subsystems of the Yovi System and how they interact to serve the end user.

[plantuml]
----
@startuml
actor "User" as user

package "Yovi System Boundary" {
    component "API Gateway\n(Nginx)" as gateway
    component "Frontend SPA\n(React)" as frontend
    component "Backend Microservices\n(Node.js & Rust)" as backend
    database "Data Persistence\n(MongoDB)" as db
}

user --> gateway : HTTPS / WSS requests
gateway --> frontend : Serves static files
gateway --> backend : Routes API & WebSockets
backend --> db : Reads / Writes
@enduml
----

Motivation::
The system follows a modular, decoupled architecture. Instead of a monolith, it is split into a static frontend, a cluster of backend microservices, and a persistent database, all shielded by a single reverse proxy gateway. This ensures security, scalability, and easier maintenance.

Contained Building Blocks::

[cols="1,3",options="header"]
|===
| **Name** | **Responsibility**
| **API Gateway** | Acts as the single point of entry, handles SSL termination, and routes traffic to the correct internal block.
| **Frontend SPA** | Client-side application running in the user's browser, responsible for the UI and UX.
| **Backend Microservices** | A collection of internal APIs that handle user authentication, real-time multiplayer networking, and heavy game logic.
| **Data Persistence** | The MongoDB database cluster that securely stores user profiles, encrypted passwords, and match statistics.
|===

=== Level 2: Backend Services Decomposition

This level zooms into the "Backend Microservices" building block, detailing the distinct autonomous services that handle users, real-time gameplay, and heavy AI computation.

[plantuml]
----
@startuml
package "Backend Microservices" {
    component "Users Service\n(Port 3000)" as users
    component "Multiplayer Service\n(Port 5000)" as multi
    component "GameY Engine\n(Port 4000)" as gamey
}

database "MongoDB" as db

users --> gamey : Internal HTTP (Bot Logic)
users ..> db : Profile & Stats queries
multi ..> db : Active match state updates
@enduml
----

Motivation::
To achieve high availability and separation of concerns, the backend is not a single server. Heavy AI calculations (Rust) are isolated from real-time socket connections (Multiplayer) and standard HTTP REST traffic (Users).

Contained Building Blocks::

[cols="1,4", options="header"]
|===
| **Building Block** | **Description**

| **Users Service**
| *Responsibility:* Manages user registration, JWT authentication, and historical statistics retrieval. It acts as a standard REST API. +
*Technology:* Node.js, Express, Mongoose. +
*Location:* `/users` directory.

| **Multiplayer Service**
| *Responsibility:* Maintains stateful, real-time WebSocket connections with active players, managing game rooms and broadcasting piece movements instantly. +
*Technology:* Node.js, Socket.io. +
*Location:* `/multiplayer` directory.

| **GameY Engine**
| *Responsibility:* Implements the core logic of the Game of Y. It validates moves, calculates bot strategies (Monte Carlo, Minimax, etc.), and checks win conditions using the YEN notation. +
*Technology:* Rust. +
*Location:* `/gamey` directory.
|===